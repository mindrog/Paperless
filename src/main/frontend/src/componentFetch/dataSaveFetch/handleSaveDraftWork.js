import React from 'react'

function handleSaveDraftWork({
  reportTitle,
  reportContent,
  reportDate,
  repoStartTime,
  repoEndTime,
  reportStatus,
  selectedApprovers,
  selectedReferences,
  selectedReceivers,
  files,
  token,
  setIsSaved,
  setSaveDate,
  setShowAlert
}) {
  return (
    <div>handleSaveDraft</div>
  )
}

export default handleSaveDraftWork